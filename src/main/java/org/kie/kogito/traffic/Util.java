package org.kie.kogito.traffic;

import java.util.Date;
import java.util.Map;

import org.kie.kogito.internal.process.runtime.KogitoProcessContext;

public class Util {

    public static void debug(Driver driver, Date date)
    {
        var x = 0;
    }
    public static void callBpmn(KogitoProcessContext kcontext){
        // var base = kcontext.getKogitoProcessRuntime().getKieBase();
        // var x = 0;
        // kcontext.getKogitoProcessRuntime().createProcessInstance("HelloWorld", Map.of("suspended", "hello"));
        // kcontext.getKogitoProcessRuntime().startProcess("HelloWorld", Map.of("suspended", "hello"));
    }
}
